# USD Dependencies

This directory is populated during the build to include the USD Python bindings
for supported Substance Painter Python versions.

Do not commit binaries here. Use `python tools/build_plugin.py` or the GitHub
release artifact, which bundles the required USD libraries.
